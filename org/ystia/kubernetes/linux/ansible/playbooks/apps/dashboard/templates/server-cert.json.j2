{
    "CN": "{{ansible_hostname}}",
    "hosts": [
        "127.0.0.1",
        "localhost",
{% for ip in masters_private_IPs %}
        "{{ip}}",
{% endfor %}
{% for ip in masters_public_IPs %}
        "{{ip}}",
{% endfor %}
        "{{ansible_fqdn}}",
        "{{ansible_hostname}}"
    ],
    "key": {
        "algo": "rsa",
        "size": 2048
    },
    "names": [
        {
            "C": "US",
            "L": "CA",
            "ST": "San Francisco"
        }
    ]
}
